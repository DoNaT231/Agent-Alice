export const EMAIL_DRAFT_SYSTEM_PROMPT =
  'Draft professional emails and return JSON with to, companyName, subject, and body fields only.'

export function buildEmailDraftUserPrompt(prompt: string): string {
  return prompt
}
